# Counterfactual inputs

This directory records the intended fault-injection boundary. Executable case
records in `../fixtures/` set `counterfactual: true` and retain evaluator-owned
truth separately; future generated variants may be stored here without changing
the model packet authority.
