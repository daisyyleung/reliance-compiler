# Assumption register

Register every material interpretation that was not directly supplied by
evidence. Include affected claims, why it was needed, supporting evidence IDs,
materiality, a verification method, and status (`supported`, `unsupported`,
`partially_supported`, `not_testable`, or `superseded`). An assumption is never
silently upgraded to evidence.
