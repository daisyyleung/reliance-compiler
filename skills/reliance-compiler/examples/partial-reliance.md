# Partial reliance example

An output can contain both `RELY` and `VERIFY` claims. For example, the
requester and requested document may be supported while an inferred sentiment
or exact deadline is not. Render each status separately and do not collapse the
packet to one score.
